---
title: Cardcloud
description: Endpoints propios de Cardcloud y proxy hacia la API externa.
---

# Cardcloud

Base path: `/api/cardcloud`

Este modulo concentra Cardcloud en una sola API local:

- Endpoints proxy hacia la API externa de Cardcloud.
- `POST /api/cardcloud/sync` para sincronizar el espejo local global de stock.
- Acciones locales para asignar o desasignar stock Cardcloud a subcompanias.

No existe un controller separado para stock. La tabla local se mantiene por sincronizacion y no expone CRUD manual.

## Endpoints

| Metodo  | Ruta                                         | Permiso                                     | Alcance  | Descripcion                        |
| ------- | -------------------------------------------- | ------------------------------------------- | -------- | ---------------------------------- |
| `POST`  | `/api/cardcloud/sync`                        | `cardcloud.sync`                            | Sistema  | Sincroniza espejo local de stock.  |
| `PATCH` | `/api/cardcloud/:id/assign-sub-company`      | `cardcloud.sub-company.assign`              | Compania | Asigna stock a una subcompania.    |
| `PATCH` | `/api/cardcloud/:id/unassign-sub-company`    | `cardcloud.sub-company.unassign`            | Compania | Quita stock de una subcompania.    |
| `GET`   | `/api/cardcloud/account`                     | `cardcloud.account.read-one`                | Sistema  | Consulta la cuenta Cardcloud.      |
| `GET`   | `/api/cardcloud/account/movements`           | `cardcloud.account.movements.read-list`     | Sistema  | Lista movimientos de cuenta.       |
| `POST`  | `/api/cardcloud/account/transfer`            | `cardcloud.account.transfer`                | Sistema  | Ejecuta una transferencia.         |
| `POST`  | `/api/cardcloud/account/transfer-bulk`       | `cardcloud.account.transfer.bulk`           | Sistema  | Ejecuta transferencias en lote.    |
| `POST`  | `/api/cardcloud/account/cards/assign`        | `cardcloud.cards.assign`                    | Sistema  | Asigna tarjetas por tipo/cantidad. |
| `POST`  | `/api/cardcloud/account/cards/assign-bulk`   | `cardcloud.cards.assign.bulk`               | Sistema  | Asigna tarjetas especificas.       |
| `GET`   | `/api/cardcloud/cards/:uuid`                 | `cardcloud.cards.read-one`                  | Sistema  | Consulta detalle de tarjeta.       |
| `GET`   | `/api/cardcloud/cards/:uuid/movements`       | `cardcloud.cards.movements.read-list`       | Sistema  | Lista movimientos de tarjeta.      |
| `GET`   | `/api/cardcloud/cards/movement/:uuid`        | `cardcloud.cards.movements.read-one`        | Sistema  | Consulta detalle de movimiento.    |
| `GET`   | `/api/cardcloud/cards/:uuid/sensitive`       | `cardcloud.cards.sensitive.read-one`        | Sistema  | Consulta datos sensibles.          |
| `GET`   | `/api/cardcloud/cards/:uuid/cvv`             | `cardcloud.cards.cvv.read`                  | Sistema  | Consulta CVV temporal.             |
| `PATCH` | `/api/cardcloud/cards/:uuid/nip`             | `cardcloud.cards.nip.update`                | Sistema  | Actualiza NIP de tarjeta.          |
| `POST`  | `/api/cardcloud/cards/validate`              | `cardcloud.cards.validate`                  | Sistema  | Valida tarjeta.                    |
| `POST`  | `/api/cardcloud/cards/:uuid/block`           | `cardcloud.cards.block`                     | Sistema  | Bloquea tarjeta.                   |
| `POST`  | `/api/cardcloud/cards/:uuid/unblock`         | `cardcloud.cards.unblock`                   | Sistema  | Desbloquea tarjeta.                |
| `GET`   | `/api/cardcloud/subaccounts`                 | `cardcloud.subaccounts.read-list`           | Sistema  | Lista subcuentas.                  |
| `GET`   | `/api/cardcloud/subaccounts/:uuid`           | `cardcloud.subaccounts.read-one`            | Sistema  | Consulta subcuenta.                |
| `GET`   | `/api/cardcloud/subaccounts/:uuid/cards`     | `cardcloud.subaccounts.cards.read-list`     | Sistema  | Lista tarjetas de subcuenta.       |
| `POST`  | `/api/cardcloud/subaccounts`                 | `cardcloud.subaccounts.create`              | Sistema  | Crea subcuenta.                    |
| `GET`   | `/api/cardcloud/subaccounts/:uuid/movements` | `cardcloud.subaccounts.movements.read-list` | Sistema  | Lista movimientos de subcuenta.    |

## Sincronizar espejo local

`POST /api/cardcloud/sync`

Requiere `RequireSystemAccess` y permiso `cardcloud.sync`.

Sincroniza todas las tarjetas disponibles desde Cardcloud hacia el espejo local global de stock. No usa `X-Company-Id` porque todavia no esta asignando operacion a una compania o subcompania.

No sincroniza datos sensibles como CVV, NIP o PAN completo.

Response `200`:

```json
{
    "synced": 20,
    "skipped": 0,
    "removed": 1
}
```

Reglas:

- Si Cardcloud no esta configurado, devuelve `400`.
- `providerStatus` se normaliza a `active`, `inactive` o `suspended`.
- `skipped` cuenta tarjetas sin ID externo util o duplicadas.
- Las tarjetas que ya no vengan en Cardcloud se marcan como `inactive` en el espejo local.
- El sync actualiza datos del proveedor (`maskedPan`, `clientId`, `balance`, `providerStatus`, `syncedAt`) sin cambiar la asignacion local a subcompania.

## Asignacion local de stock

Estas acciones no llaman a la API externa. Solo administran a que subcompania pertenece una tarjeta dentro del espejo local.

`PATCH /api/cardcloud/:id/assign-sub-company`

Requiere `X-Company-Id` y permiso `cardcloud.sub-company.assign`.

Body:

| Campo          | Tipo     | Requerido | Descripcion                         |
| -------------- | -------- | --------- | ----------------------------------- |
| `subCompanyId` | `string` | Si        | Subcompania destino del stock local. |

Response `200`:

```json
{
    "id": "stock-id",
    "subCompanyId": "sub-company-id",
    "externalId": "CARD-001",
    "assignedCardId": null,
    "maskedPan": "**** **** **** 1001",
    "clientId": "CLIENT-001",
    "balance": "1000.00",
    "providerStatus": "active",
    "syncedAt": "2026-07-09T12:00:00.000Z",
    "assignedCard": null,
    "subCompany": {
        "id": "sub-company-id",
        "companyId": "company-id",
        "key": "DEMO-SUB-01",
        "name": "Demo Sub Company 01"
    }
}
```

Reglas:

- `subCompanyId` debe pertenecer a la compania seleccionada con `X-Company-Id`.
- Si el usuario esta limitado a subcompanias, `subCompanyId` debe estar dentro de su alcance.
- El stock puede estar sin subcompania o pertenecer a una subcompania dentro del mismo alcance. No se puede reasignar stock ya asociado a otra compania o subcompania fuera del alcance.
- Si la subcompania no pertenece al alcance, devuelve `400`.
- Si el stock no existe o no es reasignable dentro del alcance, devuelve `404`.

`PATCH /api/cardcloud/:id/unassign-sub-company`

Requiere `X-Company-Id` y permiso `cardcloud.sub-company.unassign`.

No recibe body.

Response `200`: mismo formato que asignacion, con `subCompanyId: null` y `subCompany: null`.

Reglas:

- Solo puede quitar la asignacion de stock que actualmente pertenece a la compania/subcompania del alcance.
- No puede quitar asignaciones de otra compania.

## Cuenta

`GET /api/cardcloud/account`

Requiere `RequireSystemAccess`.

Response `200`: respuesta cruda de Cardcloud para `/v1/account`.

`GET /api/cardcloud/account/movements`

Requiere `RequireSystemAccess`.

Query params opcionales:

| Campo  | Tipo     | Descripcion                       |
| ------ | -------- | --------------------------------- |
| `from` | `string` | Timestamp Unix inicial, segundos. |
| `to`   | `string` | Timestamp Unix final, segundos.   |

Response `200`: respuesta cruda de Cardcloud para `/v1/account/movements`.

## Transferencias

`POST /api/cardcloud/account/transfer`

Requiere `RequireSystemAccess`.

Body:

| Campo             | Tipo                            | Requerido | Descripcion         |
| ----------------- | ------------------------------- | --------- | ------------------- |
| `sourceType`      | `account \| subaccount \| card` | Si        | Origen.             |
| `source`          | `string`                        | Si        | ID externo origen.  |
| `destinationType` | `account \| subaccount \| card` | Si        | Destino.            |
| `destination`     | `string`                        | Si        | ID externo destino. |
| `amount`          | `number`                        | Si        | Monto.              |
| `description`     | `string`                        | Si        | Descripcion.        |

Si `amount` es negativo, backend invierte origen/destino y envia el monto absoluto a Cardcloud, igual que BackendGas.

Response `200`: respuesta cruda de Cardcloud para `/v1/transfer`.

`POST /api/cardcloud/account/transfer-bulk`

Requiere `RequireSystemAccess`.

Body:

```json
{
    "transfers": [
        {
            "sourceType": "account",
            "source": "ACCOUNT-ID",
            "destinationType": "card",
            "destination": "CARD-ID",
            "amount": 100,
            "description": "Carga inicial"
        }
    ]
}
```

Response `200`:

```json
{
    "results": [
        {
            "index": 0,
            "success": true,
            "data": {}
        }
    ],
    "summary": {
        "total": 1,
        "succeeded": 1,
        "failed": 0
    }
}
```

## Tarjetas

`GET /api/cardcloud/cards/:uuid`

Response `200`: respuesta cruda de Cardcloud para `/v1/card/:uuid`.

`GET /api/cardcloud/cards/:uuid/movements`

Query params opcionales: `from`, `to`.

Response `200`: respuesta cruda de Cardcloud para `/v1/card/:uuid/movements`.

`GET /api/cardcloud/cards/movement/:uuid`

Response `200`: respuesta cruda de Cardcloud para `/v1/card/movement/:uuid`.

`GET /api/cardcloud/cards/:uuid/sensitive`

Response `200`: datos sensibles devueltos por Cardcloud.

`GET /api/cardcloud/cards/:uuid/cvv`

Response `200`: CVV temporal devuelto por Cardcloud.

`PATCH /api/cardcloud/cards/:uuid/nip`

Body:

| Campo     | Tipo     | Requerido |
| --------- | -------- | --------- |
| `old_nip` | `string` | Si        |
| `new_nip` | `string` | Si        |

Response `200`: respuesta cruda de Cardcloud para `/v1/card/:uuid/update_nip`.

`POST /api/cardcloud/cards/validate`

Body:

| Campo  | Tipo     | Requerido | Descripcion                         |
| ------ | -------- | --------- | ----------------------------------- |
| `card` | `string` | Si        | Ultimos digitos requeridos por API. |
| `pin`  | `string` | Si        | PIN.                                |
| `moye` | `string` | Si        | Vencimiento en formato Cardcloud.   |

Response `200`: respuesta cruda de Cardcloud para `/card/validate`.

`POST /api/cardcloud/cards/:uuid/block`

Response `200`: respuesta cruda de Cardcloud para `/v1/card/:uuid/block`.

`POST /api/cardcloud/cards/:uuid/unblock`

Response `200`: respuesta cruda de Cardcloud para `/v1/card/:uuid/unblock`.

## Asignacion de tarjetas

`POST /api/cardcloud/account/cards/assign`

Body:

| Campo           | Tipo     | Requerido |
| --------------- | -------- | --------- |
| `subaccount_id` | `string` | Si        |
| `card_type`     | `string` | Si        |
| `quantity`      | `number` | Si        |

Response `200`: respuesta cruda de Cardcloud para `/v1/account/cards/assign`.

`POST /api/cardcloud/account/cards/assign-bulk`

Body:

| Campo           | Tipo       | Requerido |
| --------------- | ---------- | --------- |
| `subaccount_id` | `string`   | Si        |
| `cards`         | `string[]` | Si        |

Response `200`: respuesta cruda de Cardcloud para `/v1/account/cards/assign_bulk`.

## Subcuentas

`GET /api/cardcloud/subaccounts`

Response `200`: respuesta cruda de Cardcloud para `/v1/subaccounts`.

`GET /api/cardcloud/subaccounts/:uuid`

Response `200`: respuesta cruda de Cardcloud para `/v1/subaccounts/:uuid`.

`GET /api/cardcloud/subaccounts/:uuid/cards`

Query params opcionales:

| Campo  | Tipo     | Descripcion |
| ------ | -------- | ----------- |
| `page` | `string` | Pagina.     |

Response `200`: respuesta cruda de Cardcloud para `/v1/subaccounts/:uuid/cards`.

`POST /api/cardcloud/subaccounts`

Body:

| Campo         | Tipo     | Requerido |
| ------------- | -------- | --------- |
| `ExternalId`  | `string` | Si        |
| `Description` | `string` | Si        |

Response `201`: respuesta cruda de Cardcloud para `/v1/subaccounts`.

`GET /api/cardcloud/subaccounts/:uuid/movements`

Query params opcionales: `from`, `to`.

Response `200`: respuesta cruda de Cardcloud para `/v1/subaccounts/:uuid/movements`.

## Reglas y seguridad

- `CardcloudExternalService` es el unico servicio que se conecta a la API externa.
- `CardcloudService` es el servicio local del modulo y coordina proxy, normalizacion de transferencias y sincronizacion del espejo.
- Los endpoints con alcance `Sistema` no usan `X-Company-Id`; requieren acceso global de sistema y permisos `cardcloud.*`.
- `POST /api/cardcloud/sync` no usa `X-Company-Id`; requiere `RequireSystemAccess`.
- Las acciones `assign-sub-company` y `unassign-sub-company` si usan `X-Company-Id` y validan pertenencia de la subcompania a la compania seleccionada.
- No filtres datos sensibles solo en frontend. Los endpoints `sensitive`, `cvv` y `nip` deben mostrarse solo bajo accion explicita, con confirmacion, y nunca persistirse ni loguearse en frontend.

## Notas para frontend

No generes una pagina CRUD nueva por cada endpoint de Cardcloud. Los endpoints externos son acciones tecnicas del proveedor y deben exponerse solo como herramientas puntuales para usuarios globales autorizados.

La accion `Sincronizar Cardcloud` debe ser un boton/accion tecnica de administracion global, no una pagina separada ni una accion por compania. Solo mostrarla cuando `capabilities.permissions` incluya `cardcloud.sync` y el usuario tenga acceso de sistema.

Para asignar stock a una subcompania, usa `PATCH /api/cardcloud/:id/assign-sub-company` desde una accion del inventario/stock. El usuario no debe escribir `subCompanyId`; debe seleccionarlo desde un select filtrado por la compania actual.

No crees una pantalla CRUD manual para modificar datos sincronizados como `externalId`, `maskedPan`, `clientId`, `balance` o `providerStatus`. Esos campos vienen de Cardcloud y se refrescan con `sync`.
